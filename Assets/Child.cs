using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Child : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}



/*
 * Every 10 seconds a child gets into a problem,(randomized problem) if you dont get there within 8 seconds you loose.
 * when a child gets a problem a text appears with for example a child is hungry, or a child tries to escape
 * E to interact with childto solve its problem, two children can not be in same place.
 * time decreases between children getting into a problem every 30 seconds by 1 second
 * we gonna have a timer that times the time you hve survived so far
 * 
 * 
 * 
 * 
 * 
 */
